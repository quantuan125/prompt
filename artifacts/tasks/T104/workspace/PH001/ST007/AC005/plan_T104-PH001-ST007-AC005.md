---
artifact_type: 'PLAN'
planning_level: 'ACTIVITY'
initiative_id: 'T104'
initiative_code: 'CWS'
phase: '1'
stream_id: 'T104-PH001-ST007'
activity_id: 'T104-PH001-ST007-AC005'
version: '2.6.0'
date: '2026-03-12'
status: 'draft'
author: 'LLM_Consultant'
decision_owner_role: 'Client'
governance_rules: 'prompt/templates/consultant/workspace/workspace_documentation_rules.md'
parent_plan: 'prompt/artifacts/tasks/T104/workspace/PH001/ST007/plan_T104-PH001-ST007.md'
---

# PLAN: T104 (CWS) — Phase 1 / Stream `T104-PH001-ST007` / Activity `AC005`: T102 Directory Migration

## I. EXECUTIVE SUMMARY

**Purpose**: Execute a deterministic, gate-controlled migration of `prompt/artifacts/tasks/T102/**` from mixed legacy roots and mixed epic structures into a `P-STD-004`-conformant layout. AC005 absorbs the live `consultant/` root into canonical T102/T102A/T102B/T102C surfaces, relocates the historical `planner/` root into canonical archive placement, and preserves rollback/evidence discipline through dry-run and post-apply gates.

**Non-goal**: This activity does not amend `P-STD-004` or `P-STD-005`, reopen AC004 scope, or introduce new migration-tool features unless a locked AC005 task explicitly identifies a tool incompatibility that blocks execution.

---

## II. ACTIVITY OUTLINE

**Activity ID**: `T104-PH001-ST007-AC005`
**Objective**: Produce manifest-ready migration instructions, dry-run them through a client review gate, then execute live apply, cross-initiative rewrites, and baseline-plus-allowlist validation without leaving unresolved scope decisions to the developer.
**Execution Mode**: `GATED`
**Depends On**: `T104-PH001-ST007-AC004`

**Locked implementation posture**:
- `GATE-000` is the formal unblocker for `TK003` through `TK012`; it is satisfied only when the AC005 proposal GDR records an approving client decision.
- AC005 uses four manifests only: one root manifest (`TK003`) plus one manifest per epic (`TK004`–`TK006`).
- Dry-run evidence is a first-class work product. `TK007` and `TK008` MUST produce the package reviewed at `GATE-001`; no live apply work may begin before that gate passes.
- Pre-live readiness is a separate hard-stop checkpoint. `TK008.1` through `TK008.5` MUST produce the validator-baseline, allowlist, rollback-readiness, and reviewer-verification package reviewed at `GATE-002` before `TK009` may begin.
- Live apply occurs in two ordered passes only: root pass (`TK009`) then epic pass (`TK010`).
- Post-apply acceptance is `baseline + explicit allowlist`; no new AC005-caused validator failures are permitted.
- RES-ID canonicalization is in scope for AC005. The locked mapping is:
  - `T102-RES-001` ← `research-integration-workflow`
  - `T102-RES-002` ← `roadmap-viability`
  - `T102A-RES-001` ← `sps-workflow-optimization`
  - `T102C-RES-001` ← `handoff-aggregation`
  - `T102-RES-008` ← `concept-analysis`
  - `T102-RES-009` ← `request-analysis`

**Context**:
- `prompt/artifacts/tasks/T102/**`
- `prompt/scripts/migrate_initiative.py`
- `prompt/scripts/validate_initiative_structure.py`
- `prompt/scripts/archive_manager.py`
- `prompt/artifacts/tasks/P/standard/standard_P-STD-004_file-naming-and-directory-convention.md`
- `prompt/artifacts/tasks/P/standard/standard_P-STD-005_universal-id-specification.md`
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/analysis/analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md`
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/analysis/analysis_T104-PH001-ST007-AC005_res-id-canonicalization.md`
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/proposal/proposal_T104-PH001-ST007-AC005-TK002_migration-contract-decisions.md`

### Task Register

| Task | Task ID | Name | Status | Owner | Depends On | Target | Reference | Action |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| TK001 | `T104-PH001-ST007-AC005-TK001` | Freeze + authoritative T102 inventory and classification matrix | `completed` | LLM_Consultant | — | TK001 authority pack | `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` | Analysis v2.2.0 now serves as the authoritative inventory, routing, RES-ID, and execution-mechanics pack for AC005 manifest authoring. |
| TK002 | `T104-PH001-ST007-AC005-TK002` | Lock migration contract decisions | `completed` | LLM_Consultant | TK001 | Gate-000 disposition package | `proposal_T104-PH001-ST007-AC005-TK002_migration-contract-decisions.md` | Gate-000 disposition package updated with GIR-001 through GIR-011 and an approving GDR. |
| GATE-000 | `T104-PH001-ST007-AC005-GATE-000` | Client approval of migration contract decisions | `completed` | Client | TK002 | Gate unblocker for TK003-TK012 | `proposal_T104-PH001-ST007-AC005-TK002_migration-contract-decisions.md` | Proposal GDR records `APPROVE` with Decision Date `2026-03-10`; Gate-000 unblocks all downstream execution tasks. |
| TK003 | `T104-PH001-ST007-AC005-TK003` | Author T102-root manifest | `completed` | LLM_Developer | GATE-000 | Root manifest + mapping evidence | `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` | Root manifest and mapping evidence published under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/` and consumed by Gate-001 review. |
| TK004 | `T104-PH001-ST007-AC005-TK004` | Author T102A manifest | `completed` | LLM_Developer | GATE-000 | T102A manifest + mapping evidence | `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` | T102A manifest and mapping evidence published under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/` and consumed by Gate-001 review. |
| TK005 | `T104-PH001-ST007-AC005-TK005` | Author T102B manifest | `completed` | LLM_Developer | GATE-000 | T102B manifest + mapping evidence | `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` | T102B manifest and mapping evidence published under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/` and consumed by Gate-001 review. |
| TK006 | `T104-PH001-ST007-AC005-TK006` | Author T102C manifest | `completed` | LLM_Developer | GATE-000 | T102C manifest + mapping evidence | `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` | T102C manifest and mapping evidence published under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/` and consumed by Gate-001 review. |
| TK007 | `T104-PH001-ST007-AC005-TK007` | Dry-run root manifest + produce evidence package | `completed` | LLM_Developer | TK003 | Root dry-run report set | Root manifest | Root dry-run report, rollback preview, and root evidence package published for Gate-001 review. |
| TK008 | `T104-PH001-ST007-AC005-TK008` | Dry-run epic manifests + assemble coverage/completeness package | `completed` | LLM_Developer | TK004, TK005, TK006 | Epic dry-run report set + completeness matrix | Epic manifests | Epic dry-run reports, completeness matrix, and package index published for Gate-001 review. |
| GATE-001 | `T104-PH001-ST007-AC005-GATE-001` | Dry-run evidence review + client approval | `completed` | Client | TK007, TK008 | Gate-001 approval record | AC005 dry-run package | Gate-001 reassessment recorded reviewer verdict `PASS` and proposal GDR `APPROVE`; the dry-run package is closed and the activity advances to pre-live readiness work. |
| TK003.1 | `T104-PH001-ST007-AC005-TK003.1` | Resolve legacy wrapper destination collision and refresh root manifest | `completed` | LLM_Consultant + LLM_Developer | GATE-001 | Revised root manifest + revised Gate-001 evidence for wrapper disposition | Gate-001 verification + Gate-001 proposal | Completed via the refreshed `ac005a` package; archival wrapper routing is now the approved Gate-001 disposition. |
| TK003.2 | `T104-PH001-ST007-AC005-TK003.2` | Add deterministic cache-file deletion support for AC005 | `completed` | LLM_Developer | GATE-001 | Updated migration tool, tests, and root manifest delete coverage | `prompt/scripts/migrate_initiative.py` | Completed with bounded file-delete support, regression coverage, and refreshed manifest delete coverage for the retained cache artifact. |
| TK007.1 | `T104-PH001-ST007-AC005-TK007.1` | Re-run root dry-run and refresh Gate-001 package after remediation | `completed` | LLM_Developer | TK003.1, TK003.2 | Refreshed root dry-run report, completeness matrix, package index, and dev-report references | Root manifest + Gate-001 package | Completed with the refreshed `ac005a` dry-run package and remediation dev-report dated `2026-03-12`. |
| TK008.1 | `T104-PH001-ST007-AC005-TK008.1` | Capture strict pre-apply validator baseline for live T102 | `completed` | LLM_Developer | GATE-001 | Strict validator baseline report + normalized findings ledger | `prompt/scripts/validate_initiative_structure.py` | Strict pre-apply baseline report and normalized findings ledger published in `prompt/scripts/output/T104-PH001-ST007-AC005/ac005b/`. |
| TK008.2 | `T104-PH001-ST007-AC005-TK008.2` | Produce sandboxed post-migration validator preflight evidence | `completed` | LLM_Developer | TK008.1 | Sandboxed validator preflight report + validator-compatibility note | Approved AC005 manifests | Sandboxed apply reports, strict post-migration preflight report, and validator-compatibility note published for Gate-002 review. |
| TK008.3 | `T104-PH001-ST007-AC005-TK008.3` | Author locked baseline-plus-allowlist artifact | `completed` | LLM_Consultant + LLM_Developer | TK008.1, TK008.2 | Authoritative baseline/allowlist contract artifact | Validator baseline + preflight evidence | Baseline-plus-allowlist contract published as the authoritative `TK012` comparison rule. |
| TK008.4 | `T104-PH001-ST007-AC005-TK008.4` | Publish pre-apply checkpoint and rollback capture contract | `completed` | LLM_Consultant + LLM_Developer | GATE-001 | Checkpoint/rollback readiness artifact | Rollback policy + approved manifests | Checkpoint and rollback capture contract published as the required live-execution input for `TK009` and `TK010`. |
| TK008.5 | `T104-PH001-ST007-AC005-TK008.5` | Produce Gate-002 verification artifact and rebuild the client review package | `completed` | LLM_Consultant | TK008.1, TK008.2, TK008.3, TK008.4 | Gate-002 verification artifact + rebuilt gate-disposition package | AC005 pre-live readiness package | Gate-002 verification artifact published and the gate-disposition proposal rebuilt to reference the corrected reviewer package. |
| GATE-002 | `T104-PH001-ST007-AC005-GATE-002` | Pre-live execution readiness review + client approval | `completed` | Client | TK008.5 | Pre-live readiness approval record | AC005 pre-live readiness package | Proposal GDR records reviewer verdict `PASS` and client decision `APPROVE`; `TK009` is formally unblocked. |
| TK009 | `T104-PH001-ST007-AC005-TK009` | Live apply pass 1: root redistribution and archive normalization | `completed` | LLM_Developer | GATE-002 | T102 root live state + rollback manifest | Root manifest + checkpoint contract | Pre-`TK009` checkpoint captured and root live apply completed under `prompt/scripts/output/T104-PH001-ST007-AC005/live-execution/tk009-root/`; report shows `156` moves, `2` directory creations, `1` delete, emitted rollback manifest, and no completeness discrepancies. |
| TK010 | `T104-PH001-ST007-AC005-TK010` | Live apply pass 2: epic manifests | `completed` | LLM_Developer | TK009 | `T102A`, `T102B`, `T102C` live state + rollback manifest | Epic manifests | Epic live applies completed in locked order `T102A -> T102B -> T102C` under `prompt/scripts/output/T104-PH001-ST007-AC005/live-execution/tk010-epics/`; reports show `45`, `54`, and `11` moves respectively, each with emitted rollback manifest and no completeness discrepancies. |
| TK011 | `T104-PH001-ST007-AC005-TK011` | Reference rewrite + residual old-path verification | `completed` | LLM_Developer | TK010 | Rewrite report + residual scan report | Scoped rewrite package | Bounded rewrite-only manifest executed across templates, `T102`, `T104`, `T103`, and `P` active roots under `prompt/scripts/output/T104-PH001-ST007-AC005/live-execution/tk011-rewrites/`; residual scan reported `residual_paths=0` with verification/dev-report surfaces excluded. |
| TK012 | `T104-PH001-ST007-AC005-TK012` | Post-apply validation against baseline + allowlist | `completed` | LLM_Developer | TK011 | Validation summary + allowlist comparison | Locked baseline/allowlist contract + validator evidence | Strict validator report, normalized findings ledger, allowlist comparison, and validation summary published under `prompt/scripts/output/T104-PH001-ST007-AC005/live-execution/tk012-validation/`; post-apply result matched the locked contract exactly at `53` errors / `30` warnings. |
| GATE-003 | `T104-PH001-ST007-AC005-GATE-003` | Final quality review + downstream handoff | `in_progress` | Client | TK012 | Final disposition + handoff package | AC005 evidence package | Live-execution package index, developer report, Gate-003 verification artifact, and pending gate-disposition proposal are assembled; client review is the remaining step. |

---

## III. TASKS (DETAILED)

### Task TK001: Freeze + Authoritative Inventory + Classification Matrix

**Task ID**: `T104-PH001-ST007-AC005-TK001`

**Purpose**: Establish the authoritative inventory, routing rules, classification matrix, and execution mechanics for all live T102 files before any manifest authoring begins.

**Deliverable**:
- `analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` as the authoritative TK001 pack
- `analysis_T104-PH001-ST007-AC005_res-id-canonicalization.md` as the RES-ID addendum

**Scope**:
- In scope:
  - Full inventory of `prompt/artifacts/tasks/T102/**`
  - Routing rules by artifact family
  - RES-ID canonicalization mapping for non-canonical research filenames
- Out of scope:
  - Manifest JSON authoring
  - Any filesystem mutation under T102 live surfaces

**Inputs Required**:
- `prompt/artifacts/tasks/T102/**` live filesystem
- `prompt/artifacts/tasks/P/standard/standard_P-STD-004_file-naming-and-directory-convention.md`
- `prompt/artifacts/tasks/P/standard/standard_P-STD-005_universal-id-specification.md`

**Steps**:
1. Inventory all live T102 files and classify them using the locked 7-column schema.
2. Record deterministic routing rules for root, epic, research, workspace, archive, and special-case families.
3. Resolve non-canonical research IDs and register the canonical RES assignments in the analysis package.
4. Freeze execution mechanics for manifests, rollback, and rewrite scope.

**Success Criteria**:
- [x] Every live T102 file is represented by the authoritative classification/routing pack.
- [x] Root, epic, research, workspace, and archive routing rules are explicit.
- [x] All live research items have canonical RES assignments.
- [x] Manifest authoring can proceed without inventing new routing policy.

---

### Task TK002: Lock Migration Contract Decisions

**Task ID**: `T104-PH001-ST007-AC005-TK002`

**Purpose**: Convert the TK001 findings into a client-approved execution contract with no remaining migration-scope ambiguity.

**Deliverable**:
- `proposal_T104-PH001-ST007-AC005-TK002_migration-contract-decisions.md`

**Scope**:
- In scope:
  - GIR-001 through GIR-011 decision package
  - Gate-000 GDR closure
  - Downstream contract for manifest authoring, dry-run, rollback, and rewrite scope
- Out of scope:
  - Manifest authoring
  - Dry-run or live migration execution

**Inputs Required**:
- TK001 authority pack
- SES001 and SES002 session notes

**Steps**:
1. Translate TK001 findings into the disposition summary and detailed GIR register.
2. Record the approved options for routing, execution mechanics, and RES-ID canonicalization.
3. Populate the Gate-000 GDR with the client approval record.
4. Publish the proposal as the authoritative Gate-000 contract carrier.

**Success Criteria**:
- [x] GIR-001 through GIR-011 are dispositioned.
- [x] Gate-000 GDR records an approving client decision.
- [x] Downstream tasks can reference a single proposal artifact for the migration contract.

---

### GATE-000: Client Approval of Migration Contract Decisions

**Gate ID**: `T104-PH001-ST007-AC005-GATE-000`

**Entry Criteria**:
- TK002 proposal is complete and contains the full GIR-001 through GIR-011 disposition register.
- Gate-000 GDR fields are present in the proposal.

**Reviewer**: `Client`

**Exit Criteria**:
- Proposal GDR records `Client Decision = APPROVE` or `APPROVE WITH CONDITIONS`.
- All downstream tasks from `TK003` onward depend on this gate rather than on ad-hoc narrative approval.

---

### Task TK003: Author T102-Root Manifest

**Task ID**: `T104-PH001-ST007-AC005-TK003`

**Purpose**: Encode all root-scoped moves, renames, archive relocations, and special-case dispositions into a deterministic root manifest.

**Deliverable**:
- Root manifest JSON under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Human-readable root mapping evidence under the same output directory

**Scope**:
- In scope:
  - `consultant/` absorption into `T102/ssot/`, `T102/standard/`, `T102/research/`, `T102/workspace/`, and `T102/archive/`
  - `planner/` archive relocation into `T102/archive/deprecated/planner/`
  - Root research canonicalization for `T102-RES-001`, `T102-RES-002`, `T102-RES-008`, and `T102-RES-009`
  - `consultant/external/` reclassification
  - `consultant/workspace/scripts/` disposition
- Out of scope:
  - `T102A`, `T102B`, and `T102C` epic-owned migrations
  - Dry-run or live execution

**Inputs Required**:
- TK001 authority pack
- Gate-000 disposition package

**Steps**:
1. Extract all root-origin rows from the TK001 classification matrix.
2. Encode moves/renames for SPS, concept, standards, research, workspace, archives, and special cases.
3. Add paired rewrite rules for each renamed or moved path unless explicitly exempted.
4. Validate that `from != to` for all operations and that no root target path conflicts exist.
5. Emit the root manifest JSON and a readable mapping table.

**Success Criteria**:
- [ ] Root manifest covers every root-origin file in TK001.
- [ ] `consultant/` and `planner/` are fully dispositioned with no uncovered live files.
- [ ] RES-ID canonicalization entries for `T102-RES-001`, `T102-RES-002`, `T102-RES-008`, and `T102-RES-009` are present.
- [ ] No overlapping target paths or no-op operations remain.

---

### Task TK004: Author T102A Manifest

**Task ID**: `T104-PH001-ST007-AC005-TK004`

**Purpose**: Encode the full T102A migration into a deterministic epic manifest aligned to the locked routing rules.

**Deliverable**:
- T102A manifest JSON under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Human-readable T102A mapping evidence

**Scope**:
- In scope:
  - `standards/` → `standard/`
  - request/design routing into `T102A/ssot/`
  - research canonicalization for `T102A-RES-001`
  - T102A workspace normalization and comparative-analysis reclassification
- Out of scope:
  - Root-only content
  - T102B and T102C content

**Inputs Required**:
- TK001 authority pack
- TK002 Gate-000 contract

**Steps**:
1. Extract all T102A-affined rows from the classification matrix.
2. Encode directory normalization, SSOT routing, and workspace moves for T102A.
3. Rename `T102A-SPS_sps-workflow-optimization` to `T102A-RES-001`.
4. Pair each move with rewrite rules and validate for target-path conflicts.
5. Emit the manifest and mapping evidence.

**Success Criteria**:
- [ ] T102A manifest covers all T102A rows from TK001.
- [ ] `standards/` is eliminated in favor of `standard/`.
- [ ] Request/design artifacts route to `T102A/ssot/`.
- [ ] `T102A-RES-001` rename is represented with a paired rewrite rule.

---

### Task TK005: Author T102B Manifest

**Task ID**: `T104-PH001-ST007-AC005-TK005`

**Purpose**: Encode the T102B migration, including the type-first workspace conversion, into a deterministic epic manifest.

**Deliverable**:
- T102B manifest JSON under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Human-readable T102B mapping evidence

**Scope**:
- In scope:
  - T102B type-first workspace conversion
  - `standards/` → `standard/`
  - research co-location under `research/<RES-ID>/`
  - roadmap routing into `T102B/ssot/`
  - PH000 legacy cleanup for phase0 notes/files
- Out of scope:
  - Root-owned content
  - T102A and T102C content

**Inputs Required**:
- TK001 authority pack
- TK002 Gate-000 contract

**Steps**:
1. Extract all T102B-affined rows from the classification matrix.
2. Encode workspace path normalization from type-first to timeline-first structure.
3. Encode standards, roadmap, and research directory normalization for T102B.
4. Add paired rewrite rules for each move or rename.
5. Emit the manifest and mapping evidence.

**Success Criteria**:
- [ ] T102B manifest covers all T102B rows from TK001.
- [ ] Type-first workspace paths are fully converted to canonical timeline paths.
- [ ] `standards/` is replaced with `standard/`.
- [ ] Roadmap and research paths land in canonical targets with no uncovered legacy paths.

---

### Task TK006: Author T102C Manifest

**Task ID**: `T104-PH001-ST007-AC005-TK006`

**Purpose**: Encode the T102C migration, including remaining naming and placement drift, into a deterministic epic manifest.

**Deliverable**:
- T102C manifest JSON under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Human-readable T102C mapping evidence

**Scope**:
- In scope:
  - research canonicalization for `T102C-RES-001`
  - communication placement into scope-aligned timeline directories
  - raw relocation into canonical workspace raw directories
  - `notes_` versus `snotes_` cleanup where required by scope
- Out of scope:
  - Root-only content
  - T102A and T102B content

**Inputs Required**:
- TK001 authority pack
- TK002 Gate-000 contract

**Steps**:
1. Extract all T102C-affined rows from the classification matrix.
2. Encode timeline cleanup, communication placement, and raw relocation rules.
3. Rename `T102C-CONCEPT_handoff-aggregation` to `T102C-RES-001`.
4. Pair each move with rewrite rules and validate for no-op or overlapping targets.
5. Emit the manifest and mapping evidence.

**Success Criteria**:
- [ ] T102C manifest covers all T102C rows from TK001.
- [ ] `T102C-RES-001` rename is represented with a paired rewrite rule.
- [ ] Communication, raw, and notes/snotes placements are canonical and conflict-free.

---

### Task TK007: Dry-Run Root Manifest + Produce Evidence Package

**Task ID**: `T104-PH001-ST007-AC005-TK007`

**Purpose**: Execute the root manifest in dry-run mode and produce the root evidence package consumed by `GATE-001`.

**Deliverable**:
- Root dry-run report under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Root rollback preview manifest and any completeness checks

**Scope**:
- In scope:
  - Dry-run execution of the TK003 root manifest
  - Root-only evidence capture and report review
- Out of scope:
  - Epic dry-runs
  - Any live apply operation

**Inputs Required**:
- TK003 root manifest
- `prompt/scripts/migrate_initiative.py`

**Steps**:
1. Run the TK003 manifest through `migrate_initiative.py` in dry-run mode.
2. Capture the emitted report and rollback preview output.
3. Confirm that the dry-run matches the manifest mapping exactly.
4. Confirm that no uncovered root-origin files remain outside documented exemptions.

**Success Criteria**:
- [ ] Root dry-run exits successfully.
- [ ] Reported operations match the TK003 manifest exactly.
- [ ] Root coverage is complete outside documented exemptions.
- [ ] Root evidence is ready for Gate-001 review.

---

### Task TK008: Dry-Run Epic Manifests + Assemble Coverage/Completeness Package

**Task ID**: `T104-PH001-ST007-AC005-TK008`

**Purpose**: Execute the T102A/T102B/T102C manifests in dry-run mode and produce the epic evidence package and completeness matrix consumed by `GATE-001`.

**Deliverable**:
- T102A/T102B/T102C dry-run reports under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005.1/`
- Epic completeness matrix and client-ready dry-run package

**Scope**:
- In scope:
  - Dry-run execution of TK004, TK005, and TK006 manifests
  - Epic completeness/coverage checks
  - Client-ready aggregation of root + epic dry-run evidence
- Out of scope:
  - Live apply
  - Post-apply validation

**Inputs Required**:
- TK004, TK005, and TK006 manifests
- TK007 root dry-run evidence
- `prompt/scripts/migrate_initiative.py`

**Steps**:
1. Run each epic manifest through `migrate_initiative.py` in dry-run mode.
2. Capture separate reports and rollback previews for T102A, T102B, and T102C.
3. Confirm each report matches its source manifest exactly and that no uncovered epic-owned files remain outside exemptions.
4. Assemble the root and epic dry-run outputs into a single Gate-001 package with a completeness matrix.

**Success Criteria**:
- [ ] All 3 epic dry-runs exit successfully.
- [ ] Each epic report matches its source manifest exactly.
- [ ] The dry-run package proves full coverage across root and epic scopes outside exemptions.
- [ ] Gate-001 input package is complete and reviewable.

---

**Historical recycle path**:
- Gate-001 previously recorded reviewer verdict `RECYCLE`, which triggered `TK003.1`, `TK003.2`, and `TK007.1`.
- That recycle loop is now closed: the updated verification artifact records `PASS`, the proposal GDR records `APPROVE`, and the activity advances to the new pre-live readiness checkpoint before `TK009`.

---

### GATE-001: Dry-Run Evidence Review + Client Approval

**Gate ID**: `T104-PH001-ST007-AC005-GATE-001`

**Entry Criteria**:
- TK007 root dry-run package is complete.
- TK008 epic dry-run package and completeness matrix are complete.
- No uncovered live files remain outside documented exemptions.
- The live-apply sequence and rollback posture are documented in the dry-run package.

**Reviewer**: `Client`

**Exit Criteria**:
- Client approves the dry-run package and live-apply sequence.
- No unresolved completeness or conflict findings remain.
- `TK009` is formally unblocked.

---

**Historical Recycle Record**:
- **Gate Status After Reassessment**: `completed`
- **Original Recycle Trigger**: `verification_T104-PH001-ST007-AC005_gate-001.md` reviewer verdict `RECYCLE` with blocking FINDING-001 and FINDING-002.
- **Completed Remediation Tasks**: `TK003.1`, `TK003.2`, `TK007.1`
- **Closure Basis**: Root manifest/disposition updated for the legacy wrapper, deterministic cache-file deletion support added and tested, root dry-run package refreshed, and the blocking findings resolved in the reassessment.
- **Reassessment Rule Applied**: The same gate, `T104-PH001-ST007-AC005-GATE-001`, was re-presented after remediation; no derived gate ID was created.
- **Downstream State**: `TK008.1` through `TK008.5` now prepare the separate pre-live readiness package, and `TK009` remains blocked until `GATE-002` records an approving GDR.

---


### Task TK003.1: Resolve Legacy Wrapper Destination Collision And Refresh Root Manifest

**Task ID**: `T104-PH001-ST007-AC005-TK003.1`

**Purpose**: Convert the Gate-001 script-destination finding into an explicit execution contract so the root manifest no longer relies on an unapproved deviation from GIR-009.

**Deliverable**:
- Updated Gate-001 proposal/plan alignment for the legacy wrapper posture
- Revised root manifest and root mapping evidence under `prompt/scripts/output/T104-PH001-ST007-AC005/ac005a/`

**Scope**:
- In scope:
  - Final disposition for `consultant/workspace/scripts/migrate_adr_to_std.py`
  - Root-manifest and mapping-evidence refresh for that disposition
  - Gate-001 package references affected by the disposition change
- Out of scope:
  - Broader migration-script refactoring
  - Epic manifests

**Inputs Required**:
- Gate-001 verification artifact
- Gate-001 disposition proposal
- TK003 root manifest and mapping evidence

**Steps**:
1. Confirm the approved recycle-path decision for the legacy wrapper destination.
2. Update the root manifest so the legacy wrapper path matches the approved disposition exactly.
3. Refresh the root mapping evidence and any Gate-001 package references that cite the wrapper path.
4. Verify that no root-manifest move/rewrite pair remains inconsistent with the revised disposition.

**Success Criteria**:
- [x] The legacy wrapper destination is explicitly approved and no longer inferred from developer narrative.
- [x] The root manifest and mapping evidence match the approved wrapper disposition exactly.
- [x] Gate-001 package references are synchronized to the revised wrapper posture.

---

### Task TK003.2: Add Deterministic Cache-File Deletion Support For AC005

**Task ID**: `T104-PH001-ST007-AC005-TK003.2`

**Purpose**: Resolve the Gate-001 tooling incompatibility that currently prevents deterministic removal of the retained `__pycache__` artifact before TK009.

**Deliverable**:
- Updated `prompt/scripts/migrate_initiative.py`
- Updated pytest coverage under `prompt/scripts/tests/test_migrate_initiative.py`
- Updated root manifest delete operations for the retained cache artifact

**Scope**:
- In scope:
  - Bounded tool enhancement for deterministic file deletion
  - Test coverage for the new delete behavior
  - Root-manifest update for the retained cache artifact
- Out of scope:
  - Broad archive-manager or validator changes
  - Epic-manifest changes unrelated to the cache artifact

**Inputs Required**:
- Gate-001 verification artifact
- TK003 root manifest
- `prompt/scripts/migrate_initiative.py`

**Steps**:
1. Extend `migrate_initiative.py` so manifests can delete specific files deterministically while preserving current protections against unsafe directory deletes.
2. Add pytest coverage for the new delete behavior and keep the existing non-empty-directory guard.
3. Replace the retained cache-file exemption in the root manifest with an explicit delete operation.
4. Verify that the updated manifest still passes tool validation and dry-run preflight.

**Success Criteria**:
- [x] The migration tool supports the bounded delete behavior required by AC005.
- [x] Pytest coverage explicitly exercises the new file-delete path and the retained non-empty-directory failure path.
- [x] The retained cache artifact is removed from the Gate-001 exemption set and encoded deterministically in the root manifest.

---

### Task TK007.1: Re-run Root Dry-Run And Refresh Gate-001 Package After Remediation

**Task ID**: `T104-PH001-ST007-AC005-TK007.1`

**Purpose**: Rebuild the root evidence package after the Gate-001 recycle remediation so the same gate can be re-presented on refreshed evidence.

**Deliverable**:
- Refreshed root dry-run report
- Refreshed Gate-001 completeness matrix and package index
- Refreshed root rollback-preview helper and affected dev-report references

**Scope**:
- In scope:
  - Root dry-run rerun after `TK003.1` and `TK003.2`
  - Gate-001 package refresh for root-affected evidence
- Out of scope:
  - Epic manifest reruns unless root remediation proves they are necessary
  - Live apply

**Inputs Required**:
- Revised TK003 root manifest
- Updated `prompt/scripts/migrate_initiative.py`
- Existing Gate-001 package

**Steps**:
1. Re-run the root manifest through `migrate_initiative.py` in dry-run mode.
2. Refresh the root report, rollback-preview helper, completeness matrix, and package index to reflect the remediated posture.
3. Update the developer evidence summary where root-package counts or exemptions changed.
4. Confirm that the refreshed Gate-001 package no longer contains the blocking wrapper/cache mismatches.

**Success Criteria**:
- [x] Refreshed root dry-run exits successfully.
- [x] Gate-001 package no longer carries the retained cache artifact as an exemption.
- [x] Gate-001 package aligns to the approved wrapper disposition and the remediated tool behavior.

### Task TK008.1: Capture Strict Pre-Apply Validator Baseline For Live T102

**Task ID**: `T104-PH001-ST007-AC005-TK008.1`

**Purpose**: Capture the authoritative pre-apply validator result set for the current live `T102` tree so post-apply acceptance can distinguish pre-existing issues from AC005 regressions.

**Deliverable**:
- Strict validator baseline report under `prompt/scripts/output/T104-PH001-ST007-AC005/`
- Normalized findings ledger derived from the same run

**Scope**:
- In scope:
  - Strict validator run against live `prompt/artifacts/tasks/T102/`
  - Normalization of the result set into a stable comparison ledger
- Out of scope:
  - Live apply execution
  - Residual-issue disposition decisions

**Inputs Required**:
- GATE-001 approval
- `prompt/scripts/validate_initiative_structure.py`
- Current live `prompt/artifacts/tasks/T102/`

**Steps**:
1. Run `validate_initiative_structure.py --strict` against the current live `T102` tree before any live apply occurs.
2. Save the raw validator output under the AC005 output directory using the same evidence-packaging conventions as other AC005 reports.
3. Normalize the findings into a stable comparison ledger suitable for later baseline-parity checks in `TK012`.
4. Confirm the captured baseline is explicitly time-scoped as pre-`TK009` evidence.

**Success Criteria**:
- [x] A strict pre-apply validator baseline report exists under the AC005 output directory.
- [x] A normalized findings ledger exists and is reviewable.
- [x] The baseline is captured before any live apply work begins.
- [x] The evidence is sufficient to support later baseline-parity comparison.

---

### Task TK008.2: Produce Sandboxed Post-Migration Validator Preflight Evidence

**Task ID**: `T104-PH001-ST007-AC005-TK008.2`

**Purpose**: Prove before live apply that the validator can assess AC005's target canonical T102 structure and that no validator/tooling incompatibility will invalidate the baseline-plus-allowlist acceptance model.

**Deliverable**:
- Sandboxed post-migration validator preflight report
- Validator-compatibility note for canonical T102/T102A/T102B/T102C structure

**Scope**:
- In scope:
  - Validator preflight against a projected post-migration surface
  - Documentation of any validator limitations or assumptions relevant to AC005
- Out of scope:
  - Live apply execution
  - Final allowlist decisions

**Inputs Required**:
- TK008.1 strict baseline report and findings ledger
- Approved AC005 manifests
- `prompt/scripts/validate_initiative_structure.py`

**Steps**:
1. Construct a sandboxed or otherwise non-live validation target representing the approved post-migration AC005 structure.
2. Run the validator against that projected post-migration surface.
3. Record whether the validator recognizes the canonical target structure without tooling-only failure modes.
4. Publish a short compatibility note stating any constraints that must hold during `TK009`-`TK012`.

**Success Criteria**:
- [x] A sandboxed post-migration validator report exists under the AC005 output directory.
- [x] The validator-compatibility note explicitly states that AC005 can rely on validator evidence for `TK012`.
- [x] Any tooling constraints are documented before live apply.
- [x] No unresolved validator-compatibility gap remains before `GATE-002`.

---

### Task TK008.3: Author Locked Baseline-Plus-Allowlist Artifact

**Task ID**: `T104-PH001-ST007-AC005-TK008.3`

**Purpose**: Convert the baseline-plus-allowlist acceptance model into a single authoritative artifact that defines what residual validator findings are allowed after migration and what must be treated as an AC005 regression.

**Deliverable**:
- Locked baseline-plus-allowlist artifact under the AC005 workspace package

**Scope**:
- In scope:
  - Pre-apply baseline result set
  - Explicit residual allowlist entries with rationale
  - Comparison rule for `TK012`
- Out of scope:
  - Live apply execution
  - Remediation work beyond defining the acceptance contract

**Inputs Required**:
- TK008.1 strict baseline report and normalized findings ledger
- TK008.2 sandboxed validator preflight evidence
- AC005 readiness analysis and approved plan posture

**Steps**:
1. Enumerate the pre-apply validator findings that are expected to remain acceptable after migration.
2. Record each allowed residual with enough detail to support exact post-apply comparison.
3. State the governing comparison rule: any new post-apply finding outside the locked allowlist is an AC005 regression.
4. Publish the artifact as the authoritative pre-live acceptance contract for `TK012`.

**Success Criteria**:
- [x] A single authoritative baseline/allowlist artifact exists.
- [x] The allowed residual findings are explicit and reviewable.
- [x] The comparison rule for `TK012` is unambiguous.
- [x] The artifact is sufficient for client review at `GATE-002`.

---

### Task TK008.4: Publish Pre-Apply Checkpoint And Rollback Capture Contract

**Task ID**: `T104-PH001-ST007-AC005-TK008.4`

**Purpose**: Convert the rollback policy into execution-ready instructions so the pre-`TK009` checkpoint and the live rollback outputs are concrete required evidence rather than narrative intent only.

**Deliverable**:
- Pre-apply checkpoint and rollback capture contract artifact

**Scope**:
- In scope:
  - Pre-`TK009` checkpoint expectations
  - Required storage/location contract for checkpoint evidence
  - Required rollback/report outputs for `TK009` and `TK010`
- Out of scope:
  - Capturing the live checkpoint itself
  - Running live apply

**Inputs Required**:
- GATE-001 approval
- AC005 rollback policy from the readiness analysis and plan
- Approved AC005 manifests

**Steps**:
1. Define the required pre-`TK009` checkpoint contents and where that evidence must be stored.
2. Define the required rollback/report outputs for the root and epic live passes.
3. Align the contract with the locked full-rollback policy to the pre-`TK009` checkpoint.
4. Publish the artifact as a required execution input for `TK009` and `TK010`.

**Success Criteria**:
- [x] The pre-apply checkpoint contract is explicit and reviewable.
- [x] Required rollback outputs for `TK009` and `TK010` are defined.
- [x] The contract is consistent with the locked rollback policy.
- [x] No checkpoint/rollback evidence ambiguity remains before `GATE-002`.

---

### Task TK008.5: Produce Gate-002 Verification Artifact And Rebuild The Client Review Package

**Task ID**: `T104-PH001-ST007-AC005-TK008.5`

**Purpose**: Perform the evidence-first Gate-002 verification review, close the plan-authority/package-integrity scope gap, and publish the corrected reviewer package and gate-disposition proposal for client decision.

**Deliverable**:
- `verification_T104-PH001-ST007-AC005_gate-002.md`
- Rebuilt `proposal_T104-PH001-ST007-AC005-GATE-002_gir-disposition-package.md`

**Scope**:
- In scope:
  - Independent verification of `TK008.1` through `TK008.4` deliverables against the AC005 plan and workspace gate guidelines
  - Plan amendment required to introduce reviewer-owned verification authority before `GATE-002`
  - Rebuilt Gate-002 proposal package aligned to the corrected verification artifact and pending GDR state
- Out of scope:
  - Regeneration of validator or sandbox execution artifacts
  - Recording the client Gate-002 decision

**Inputs Required**:
- `TK008.1` strict baseline report and normalized findings ledger
- `TK008.2` sandboxed validator preflight evidence and compatibility note
- `TK008.3` baseline-plus-allowlist contract
- `TK008.4` checkpoint and rollback capture contract
- `prompt/templates/consultant/workspace/guideline_workspace_verification.md`
- `prompt/templates/consultant/workspace/guideline_workspace_proposal.md`

**Steps**:
1. Amend the AC005 activity plan so the Gate-002 verification work has explicit tracked authority under `TK008.5`.
2. Independently verify the Gate-002 evidence set and publish the primary verification artifact using the workspace verification template.
3. Rebuild the Gate-002 gate-disposition proposal from scratch so it references the real verification artifact and carries a compliant pending GDR.
4. Confirm the corrected package is internally consistent and that downstream gating language still blocks `TK009` until the client records an approving GDR.

**Success Criteria**:
- [x] The AC005 activity plan contains a reviewer-owned `TK008.5` task before `GATE-002`.
- [x] A primary Gate-002 verification artifact exists under the canonical activity `verification/` path.
- [x] The Gate-002 proposal references the correct verification artifact and contains a compliant pending GDR.
- [x] The rebuilt package is sufficient for client review and keeps `TK009` blocked pending Gate-002 approval.

---

### GATE-002: Pre-Live Execution Readiness Review + Client Approval

**Gate ID**: `T104-PH001-ST007-AC005-GATE-002`

**Entry Criteria**:
- TK008.1 strict validator baseline report and normalized findings ledger are complete.
- TK008.2 sandboxed post-migration validator preflight evidence is complete.
- TK008.3 locked baseline-plus-allowlist artifact is complete.
- TK008.4 checkpoint/rollback capture contract is complete.
- TK008.5 Gate-002 verification artifact and rebuilt gate-disposition package are complete.

**Reviewer**: `Client`

**Exit Criteria**:
- Client approves the strict validator baseline as the governing pre-apply reference point.
- Client approves the locked baseline-plus-allowlist artifact as the authoritative acceptance contract for `TK012`.
- Client approves the validator preflight posture and the checkpoint/rollback capture contract for live execution.
- `TK009` is formally unblocked.

---


### Task TK009: Live Apply Pass 1 — Root Redistribution And Archive Normalization

**Task ID**: `T104-PH001-ST007-AC005-TK009`

**Purpose**: Execute the approved root manifest against the live T102 tree and capture rollback/evidence outputs for the root pass.

**Deliverable**:
- Root live-apply report
- Root rollback manifest
- Updated root live state under `prompt/artifacts/tasks/T102/`

**Scope**:
- In scope:
  - Root live apply only
  - Root rollback/evidence capture
- Out of scope:
  - Epic live apply
  - Cross-initiative reference rewrites

**Inputs Required**:
- GATE-002 approval
- TK003 root manifest
- TK008.4 checkpoint and rollback capture contract

**Steps**:
1. Capture the pre-apply checkpoint required by the approved checkpoint/rollback contract.
2. Execute the TK003 manifest in live mode.
3. Capture the root live-apply report and rollback manifest.
4. Confirm the root pass completed without unresolved execution errors.

**Success Criteria**:
- [ ] Root live apply exits successfully.
- [ ] Root rollback manifest is emitted.
- [ ] Root live state matches the approved root manifest.
- [ ] No unresolved execution errors remain before epic apply begins.

---

### Task TK010: Live Apply Pass 2 — Epic Manifests

**Task ID**: `T104-PH001-ST007-AC005-TK010`

**Purpose**: Execute the approved epic manifests against the live T102A/T102B/T102C trees and capture rollback/evidence outputs for the epic pass.

**Deliverable**:
- T102A/T102B/T102C live-apply reports
- Epic rollback manifest(s)
- Updated epic live state under `prompt/artifacts/tasks/T102/`

**Scope**:
- In scope:
  - Live apply for T102A, T102B, and T102C
  - Epic rollback/evidence capture
- Out of scope:
  - Cross-initiative reference rewrites
  - Validation/allowlist comparison

**Inputs Required**:
- TK009 successful completion
- TK004, TK005, and TK006 manifests
- TK008.4 checkpoint and rollback capture contract

**Steps**:
1. Execute the epic manifests in the locked order established by Gate-000.
2. Capture live-apply reports and rollback outputs for each epic pass.
3. Confirm the epic pass completed without unresolved execution errors.
4. If any epic pass fails, treat the pre-TK009 checkpoint as the primary rollback target and ensure the emitted rollback outputs match the approved capture contract.

**Success Criteria**:
- [ ] All epic live applies exit successfully.
- [ ] Epic rollback output is emitted.
- [ ] Live state matches the approved epic manifests.
- [ ] No unresolved execution errors remain before reference rewriting begins.

---

### Task TK011: Reference Rewrite + Residual Old-Path Verification

**Task ID**: `T104-PH001-ST007-AC005-TK011`

**Purpose**: Rewrite all affected T102 path references across `prompt/**` and prove that no residual old paths remain outside documented exclusions.

**Deliverable**:
- Rewrite report
- Residual old-path scan report

**Scope**:
- In scope:
  - Cross-initiative scanning of `prompt/**`
  - Rewrite rules paired to moved or renamed T102 paths
  - Residual scan for old T102 paths after rewriting
- Out of scope:
  - Structural validation
  - Additional content changes unrelated to path updates

**Inputs Required**:
- TK009 and TK010 live apply outputs
- Manifest rewrite rules from TK003–TK006

**Steps**:
1. Run the scoped rewrite pass across `prompt/**`, excluding `prompt/scripts/output/`.
2. Capture a report of rewritten files and rules applied.
3. Run a residual scan for old T102 paths.
4. Record any documented exclusions and confirm no unexpected residuals remain.

**Success Criteria**:
- [ ] All planned rewrite rules are executed.
- [ ] Residual old-path scan returns zero outside documented exclusions.
- [ ] Cross-initiative references to moved/renamed T102 paths are repaired.

---

### Task TK012: Post-Apply Validation Against Baseline + Allowlist

**Task ID**: `T104-PH001-ST007-AC005-TK012`

**Purpose**: Prove that AC005 introduced no new migration-caused structural violations and that any remaining validator findings match the pre-locked baseline plus allowlist contract.

**Deliverable**:
- Validation summary report
- Baseline-versus-allowlist comparison

**Scope**:
- In scope:
  - Strict validator run against the migrated T102 tree
  - Comparison against the locked baseline-plus-allowlist contract
  - Final residual-issue accounting for Gate-003
- Out of scope:
  - New migration work
  - Follow-up cleanup outside the allowlist contract

**Inputs Required**:
- TK011 rewrite report and residual scan
- TK008.1 strict baseline report and normalized findings ledger
- TK008.2 sandboxed validator preflight evidence
- TK008.3 locked baseline-plus-allowlist artifact
- `prompt/scripts/validate_initiative_structure.py`

**Steps**:
1. Run the validator against the migrated T102 tree.
2. Compare the result set against the locked baseline-plus-allowlist contract.
3. Confirm that no new AC005-caused validator failures were introduced outside the locked allowlist and that the post-apply result set matches or improves on baseline parity.
4. Package the validation output for Gate-003 review.

**Success Criteria**:
- [ ] Validator run completes successfully.
- [ ] No new AC005-caused validation failures are introduced.
- [ ] Residual findings match the locked allowlist exactly.
- [ ] Gate-003 review package is complete.

---

### GATE-003: Final Quality Review + Downstream Handoff

**Gate ID**: `T104-PH001-ST007-AC005-GATE-003`

**Entry Criteria**:
- TK012 validation summary is complete.
- Root/epic live-apply reports, rollback outputs, rewrite report, residual scan, and allowlist comparison are assembled into a reviewer package.

**Reviewer**: `Client`

**Exit Criteria**:
- Final disposition is recorded for AC005.
- Evidence package is sufficient for downstream T102/P standards feedback consumption and handoff.

---

## IV. LINKS REGISTER

| Link Type | Target | Path |
|:--|:--|:--|
| Plan (this file) | AC005 Activity Plan | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/plan_T104-PH001-ST007-AC005.md` |
| Plan | ST007 Stream Plan | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/plan_T104-PH001-ST007.md` |
| Proposal | Gate-000 disposition package | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/proposal/proposal_T104-PH001-ST007-AC005-TK002_migration-contract-decisions.md` |
| Verification | Gate-002 verification artifact | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/verification/verification_T104-PH001-ST007-AC005_gate-002.md` |
| Proposal | Gate-002 disposition package | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/proposal/proposal_T104-PH001-ST007-AC005-GATE-002_gir-disposition-package.md` |
| Output | Live-execution package index | `prompt/scripts/output/T104-PH001-ST007-AC005/live-execution/index_T104-PH001-ST007-AC005_gate-003-package.md` |
| Dev-Report | TK009 through GATE-003 live execution | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/dev-report/dev-report_T104-PH001-ST007-AC005_tk009-to-gate-003-live-execution_2026-03-12.md` |
| Verification | Gate-003 verification artifact | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/verification/verification_T104-PH001-ST007-AC005_gate-003.md` |
| Proposal | Gate-003 disposition package | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/proposal/proposal_T104-PH001-ST007-AC005-GATE-003_gir-disposition-package.md` |
| Analysis | AC005 Readiness Assessment | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/analysis/analysis_T104-PH001-ST007-AC005_t102-directory-readiness.md` |
| Analysis | AC005 RES-ID Canonicalization Audit | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/analysis/analysis_T104-PH001-ST007-AC005_res-id-canonicalization.md` |
| Notes | ST007 Notes Register | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/notes_T104-PH001-ST007.md` |
| Session Notes | AC005 SES001 | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/snotes/snotes_T104-PH001-ST007-AC005-SES001.md` |
| Session Notes | AC005 SES002 | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC005/snotes/snotes_T104-PH001-ST007-AC005-SES002.md` |

---

## V. CHANGELOG

| Version | Date | Type | Summary |
|:--|:--|:--|:--|
| v2.7.0 | 2026-03-12 | Amendment | Recorded live execution completion through `TK012`: captured the pre-`TK009` checkpoint, applied the root and epic manifests, executed bounded cross-initiative reference rewrites with zero residual old-path matches, validated post-apply parity at `53` errors / `30` warnings against the locked allowlist contract, and assembled the `GATE-003` reviewer/client handoff package. |
| v2.6.0 | 2026-03-12 | Amendment | Recorded Gate-002 passage after the rebuilt reviewer package. Marked `GATE-002` complete with approving GDR state, added the Gate-002 verification/proposal artifacts to the links register, and synchronized the pre-live readiness housekeeping so `TK009` is now formally unblocked. |
| v2.5.0 | 2026-03-12 | Amendment | Added `TK008.5` as the reviewer-owned Gate-002 verification task, synchronized `TK008.1` through `TK008.4` to completed status based on the published pre-live evidence package, moved `GATE-002` to `in_progress`, and rebuilt the gate package authority chain so the Gate-002 verification artifact precedes the client decision record. |
| v2.4.0 | 2026-03-12 | Amendment | Added a new hard-stop pre-live readiness stage before `TK009`. Inserted `TK008.1` through `TK008.4` for validator baseline capture, validator preflight, locked allowlist authoring, and checkpoint/rollback contract publication; added blocking `GATE-002`; renumbered the former final handoff gate to `GATE-003`; and updated `TK009`-`TK012` to consume the new pre-live artifacts. |
| v2.3.0 | 2026-03-12 | Amendment | Recorded Gate-001 passage after reassessment. Marked `GATE-001`, `TK003.1`, `TK003.2`, and `TK007.1` complete, updated the recycle block to a historical closure record, and synchronized the task register with the approving Gate-001 GDR. |
| v2.2.0 | 2026-03-12 | Amendment | Normalized Gate-001 recycle semantics to match the updated workspace guidelines. Corrected frontmatter version, marked TK003-TK008 complete, set `GATE-001` to `in_progress`, replaced invalid `GATE-001 verification` dependencies with `GATE-001`, and added an explicit `Recycle Re-entry Block` for the same-gate reassessment loop. |
| v2.1.0 | 2026-03-11 | Minor Amendment | Recorded Gate-001 verification/disposition status, marked TK003-TK008 complete, added recycle-path tasks `TK003.1`, `TK003.2`, and `TK007.1`, and clarified that TK009 remains blocked until the Gate-001 findings are closed and the GDR is approving. |
| v2.0.0 | 2026-03-11 | Major Amendment | Rewrote AC005 to match the activity-plan template, added `GATE-000`, split dry-run production into `TK007`/`TK008` before `GATE-001`, renumbered downstream execution to `TK009`–`TK012`, and aligned the task register with the approved Gate-000 proposal and RES-ID canonicalization decisions. |
| v1.0.0 | 2026-03-09 | Initial | Dedicated AC005 activity plan created. Refactored AC005 from inline stream-plan decomposition to standalone activity plan with readiness-first sequencing, locked root dispositions, baseline-plus-allowlist validation, and two-gate evidence model. |
