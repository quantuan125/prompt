---
artifact_type: 'PLAN'
planning_level: 'ACTIVITY'
initiative_id: 'T104'
initiative_code: 'CWS'
phase: '1'
stream_id: 'T104-PH001-ST007'
activity_id: 'T104-PH001-ST007-AC004'
version: '2.2.0'
date: '2026-03-02'
status: 'draft'
author: 'LLM_Developer'
decision_owner_role: 'Client'
governance_rules: 'prompt/templates/consultant/workspace/workspace_documentation_rules.md'
parent_plan: 'prompt/artifacts/tasks/T104/workspace/PH001/ST007/plan_T104-PH001-ST007.md'
---

# PLAN: T104 (CWS) / PH001 / ST007 / AC004 - Script Enhancement + P Directory Migration

## I. Executive Summary

**Purpose**: Enhance migration/validation/scaffold scripts for P-initiative compatibility, then execute P directory restructuring (inventory-driven; type-first -> timeline-first conversion). The P initiative is the second migration target after T104 (AC001), exercising the same toolchain with enhancements for existing-directory merge and pre-migration validation profiles.

**Non-goal**: This activity does not address T102 migration (AC005) or archive tooling (AC003). Migrator script (`migrate_initiative.py`) works as-is and is not enhanced in this activity.

## II. Activity Outline

**Activity ID**: `T104-PH001-ST007-AC004`
**Objective**: Deliver a fully convention-conformant P initiative directory with 0 validation errors and 0 broken references.
**Execution Mode**: GATED (dry-run approval before live apply)
**Depends On**: AC001 (completed)

**Rollback policy (locked)**:
- Primary rollback guarantee: restore to a known-good checkpoint (git commit hash or out-of-tree snapshot) captured immediately pre-apply
- Secondary rollback aid: `migrate_initiative.py` rollback manifest (moves only; does not revert content rewrites)

## III. Locked Decisions (Commissioning Complete)

| ID | Decision | Status | Owner |
|:--|:--|:--|:--|
| DEC-AC004-001 | Roadmap canonical location is `ssot/` (not workspace). | Confirmed | Client |
| DEC-AC004-002 | Proposal is “both” by navigation: canonical stream-scoped proposal file + activity-scoped pointer artifact (no duplicated proposal content). | Confirmed | Client |
| DEC-AC004-003 | `--profile pre-migration` suppresses noise only (type-first workspace dirs, root `raw/`); missing required roots and missing SSOT `concept_` remain errors. | Confirmed | Client |
| DEC-AC004-004 | `concept_P-PROGRAM.md` is created manually (outside `migrate_initiative.py`). | Confirmed | Client |
| DEC-AC004-005 | Rollback uses a known-good checkpoint as the guarantee; rollback manifest is supplemental. | Confirmed | Client |

**Context**:
- `prompt/artifacts/tasks/P/**` - migration target
- `prompt/scripts/validate_initiative_structure.py` - enhancement target
- `prompt/scripts/scaffold_initiative.py` - enhancement target
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/analysis/analysis_T104-PH001-ST007-AC004_p-directory-readiness.md` - primary input (stream-level)
- `prompt/artifacts/tasks/T104/workspace/PH001/ST002/AC000/proposal/proposal_T104-PH001-ST002-AC000_directory-naming-convention.md` - authority surface (v3.4.0)

## IV. Task Register

| Task | Task ID | Name | Status | Owner | Depends On | Target | Action |
|:--|:--|:--|:--|:--|:--|:--|:--|
| TK001 | `T104-PH001-ST007-AC004-TK001` | Freeze + inventory P filesystem; lock remaining placement/rollback details | `completed` | LLM_Developer | — | P inventory + decision locks | Rebaseline freeze + authoritative inventory captured in `dev-report_T104-PH001-ST007-AC004_2026-02-23.md` (Section VIII; 49 files as-of `2026-02-24T06:00:10+07:00`). |
| TK002 | `T104-PH001-ST007-AC004-TK002` | Enhance validator: `--profile pre-migration` flag | `completed` | LLM_Developer | — | `validate_initiative_structure.py` | Implemented + behavior-evidenced in `dev-report_T104-PH001-ST007-AC004_2026-02-23.md` (Section IV) + `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/validation_*`. |
| TK003 | `T104-PH001-ST007-AC004-TK003` | Enhance scaffold: `--force` merge mode + `--epic-ids` flag | `completed` | LLM_Developer | — | `scaffold_initiative.py` | Implemented + behavior-evidenced in `dev-report_T104-PH001-ST007-AC004_2026-02-23.md` (Section V) + `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/validation_*`. |
| TK004 | `T104-PH001-ST007-AC004-TK004` | Author P migration manifest (inventory-driven: type-first elimination, snotes_ rename, raw rename, research mkdir, proposal pointer) | `completed` | LLM_Developer | TK001 | Manifest JSON + mapping evidence | Manifest updated at `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/manifest_T104-PH001-ST007-AC004_p-migration.json` + mapping embedded in dev-report (Section III). |
| TK004.1 | `T104-PH001-ST007-AC004-TK004.1` | Remediation: Convention compliance + `--report-path` hardening | `completed` | LLM_Developer | TK004 | AC004 artifacts + all `--report-path` scripts | Convention remediation + report-path guardrail proof recorded in `dev-report_T104-PH001-ST007-AC004_2026-02-23.md` (Sections VII–VIII). |
| GATE-001 | `T104-PH001-ST007-AC004-GATE-001` | Dry-run evidence review + Client approval | `completed` | Client | TK002, TK003, TK004, TK004.1 | — | Client approved progression to TK005 commissioning. |
| TK005 | `T104-PH001-ST007-AC004-TK005` | Live apply P migration | `completed` | LLM_Developer | GATE-001 | `prompt/artifacts/tasks/P/**` | Applied migration via `ac004.1` manifest; report + rollback manifest in `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.2/`. |
| TK006 | `T104-PH001-ST007-AC004-TK006` | Post-apply strict validation (0 errors) | `completed` | LLM_Developer | TK005 | Validation report | Strict validation passed with `0` errors after post-apply remediation (`research/report` -> `research/P-RES-001/report_*`). |
| TK007 | `T104-PH001-ST007-AC004-TK007` | Reference update pass for P paths | `completed` | LLM_Developer | TK005 | Reference rewrite report | Residual path scan completed with `residual_paths=0` across `prompt` markdown files (see `ac004.2` residual scan report). |
| GATE-002 | `T104-PH001-ST007-AC004-GATE-002` | Post-migration final quality gate | `in_progress` | Client | TK005, TK006, TK007 | — | Technical execution package complete; formal verification artifact deferred to `LLM_Reviewer`. |

## V. Tasks (Detailed)

### Task TK001: Freeze + Inventory + Final Commissioning Locks

**Task ID**: `T104-PH001-ST007-AC004-TK001`

**Purpose**: Freeze the P filesystem during commissioning, inventory the live P directory (source of truth for manifest coverage), and confirm remaining commissioning locks so manifest authoring is deterministic.

**Deliverable**:
- `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/inventory_T104-PH001-ST007-AC004_p-files.md` (authoritative P file inventory + counts)
- Confirmed issue register: all issues marked as actionable or explicitly deferred
- Commissioning lock record confirming DEC-AC004-001 through DEC-AC004-005 (may be embedded in inventory file)

**Inputs Required**:
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/analysis/analysis_T104-PH001-ST007-AC004_p-directory-readiness.md` - primary input (stream-level)
- `prompt/artifacts/tasks/P/**` - live filesystem

**Steps**:
1. Declare a freeze window on `prompt/artifacts/tasks/P/**` from inventory start through GATE-001 approval (no manual edits/moves while manifest is authored and reviewed)
2. Build authoritative file inventory (sorted path list + counts by extension). Treat this inventory (not any prior estimate) as manifest coverage truth.
3. Read analysis issue register (P-ISS-001 through P-ISS-011) and cross-check against the live P filesystem
4. Resolve SES### token assignment for `raw_P-PH000-ST000-AC001-2026-02-05.txt` (assign SES001)
5. Reconfirm locked placement semantics:
   - Roadmap canonical location: `ssot/` (DEC-AC004-001)
   - Proposal “both” semantics: canonical stream-scoped proposal + activity-scoped pointer artifact (DEC-AC004-002)
6. Reconfirm rollback policy and required checkpoint evidence for GATE-001 (DEC-AC004-005)
7. Flag any residual issues requiring client consultation (expected: none)

**Success Criteria**:
- [ ] All 11 issues reviewed and confirmed actionable
- [ ] SES### token assignment resolved for date-based raw file
- [ ] Inventory report exists and is referenced by TK004
- [ ] No unresolved ambiguities remain (commissioning complete)

### Task TK002: Enhance Validator - `--profile pre-migration`

**Task ID**: `T104-PH001-ST007-AC004-TK002`

**Purpose**: Add a `--profile pre-migration` flag to `validate_initiative_structure.py` that suppresses false-positive *noise* when validating a pre-migration directory (type-first workspace dirs and root-level raw), while keeping hard blockers as errors.

**Deliverable**:
- Enhanced `validate_initiative_structure.py` with `--profile` argument

**Inputs Required**:
- `prompt/scripts/validate_initiative_structure.py` - current source

**Scope**:
- In scope:
  - `--profile pre-migration` flag
  - Suppress checks: `_validate_no_type_first_dirs`, `_validate_no_root_raw` when profile is `pre-migration`
  - Default profile: `post-migration` (all checks enabled, backward compatible)
  - Profile does not change “required roots” and SSOT requirements:
    - Missing `research/` remains an error (required root)
    - Missing SSOT `concept_*.md` remains an error
- Out of scope:
  - New validation checks
  - Changes to existing check logic (only conditional gating)

**Steps**:
1. Add `--profile` CLI argument with choices `[post-migration, pre-migration]`, default `post-migration`
2. Pass profile to `InitiativeValidator.__init__`
3. Gate `_validate_no_type_first_dirs` and `_validate_no_root_raw` calls on profile != `pre-migration`
4. Verify: run against P with `--profile pre-migration` (expect reduced noise, but hard blockers still present until remediated); run against T104 with default profile (expect 0 errors - no regression)

**Success Criteria**:
- [ ] `--profile pre-migration` suppresses type-first and root-raw errors
- [ ] Default behavior unchanged (backward compatible)
- [ ] Running against T104 post-migration: 0 errors (no regression)

### Task TK003: Enhance Scaffold - `--force` and `--epic-ids`

**Task ID**: `T104-PH001-ST007-AC004-TK003`

**Purpose**: Add `--force` (merge mode) and `--epic-ids` flags to `scaffold_initiative.py` so it can generate missing directories in an existing initiative without aborting.

**Deliverable**:
- Enhanced `scaffold_initiative.py` with `--force` and `--epic-ids` arguments

**Inputs Required**:
- `prompt/scripts/scaffold_initiative.py` - current source

**Scope**:
- In scope:
  - `--force` flag: skip the `if plan.root.exists(): return 1` hard-stop; create only missing directories
  - `--epic-ids` flag: accept comma-separated epic IDs (e.g., `T104A,T104B`) and generate sub-initiative root structures
- Out of scope:
  - Archive structure scaffolding (AC003 scope)
  - File generation (scaffold only creates directories)

**Steps**:
1. Add `--force` flag to argparse
2. Modify line 131 guard: when `--force` is set, skip existence check; `mkdir(parents=True, exist_ok=True)` handles merging naturally
3. Add `--epic-ids` argument (comma-separated)
4. For each epic ID: generate `<tasks_root>/<epic_id>/` with same root structure (`archive/`, `research/`, `ssot/`, `standard/`, `workspace/`)
5. Verify (pre-gate, non-destructive): run `--force --apply` against a scratch path (not `prompt/artifacts/tasks/P/**`) to prove merge behavior; use dry-run evidence for live P pre-gate

**Success Criteria**:
- [ ] `--force` does not abort on existing initiative root
- [ ] Only missing directories are created (existing untouched)
- [ ] `--epic-ids` generates sub-initiative root structures
- [ ] Dry-run mode works correctly with both new flags

### Task TK004: Author P Migration Manifest

**Task ID**: `T104-PH001-ST007-AC004-TK004`

**Purpose**: Author a complete, deterministic migration manifest (JSON) mapping the entire inventoried P filesystem from current (type-first) to target (timeline-first) locations. Include `snotes_` renames, raw naming fixes, and required directory creation operations.

**Deliverable**:
- `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/manifest_T104-PH001-ST007-AC004_p-migration.json` - manifest file
- `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/inventory_T104-PH001-ST007-AC004_p-files.md` - inventory evidence (input to coverage check)
- Optional (recommended for GATE review): `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/mapping_T104-PH001-ST007-AC004_p-files.md` - human-readable mapping table (current -> target)

**Inputs Required**:
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/analysis/analysis_T104-PH001-ST007-AC004_p-directory-readiness.md` - issue register (stream-level)
- `prompt/artifacts/tasks/P/**` - live filesystem
- `prompt/artifacts/tasks/T104/workspace/PH001/ST002/AC000/proposal/proposal_T104-PH001-ST002-AC000_directory-naming-convention.md` - authority surface

**Scope**:
- In scope:
  - All inventoried files considered; manifest encodes only required operations (no no-op moves)
  - `mkdir` operations for new timeline directories
  - `move` operations for type-first -> timeline relocation
  - `rename` operations for `snotes_` prefix and raw naming fixes
  - `reference_rewrites` entries for all moved/renamed paths (schema: `{old,new}`; key name is plural)
  - Proposal “both” semantics implementation:
    - Canonical proposal remains stream-scoped
    - Activity-level `proposal/` contains a pointer artifact (link-only; no duplicated proposal content)
- Out of scope:
  - Archive file (`archive/changelog_roadmap_P-PROGRAM_phase0.md`) - no action per SES002 decision
  - Content modifications, except reference rewrites (frontmatter updates are reference-rewrite-only)
  - Creating new content files via migrator operations (no `create` action); concept stub is handled manually (DEC-AC004-004)

**Steps**:
1. Build current-path inventory from live filesystem
2. Apply convention rules to derive target paths (per analysis issue register)
3. Generate `mkdir` operations for all new directories
4. Generate `move` operations with deterministic ordering (deepest paths first)
5. Generate `rename` operations for prefix changes (`notes_` -> `snotes_`, raw naming fixes)
6. Generate `reference_rewrites` entries paired 1:1 with each move/rename
7. Validate manifest: 0 no-op moves (from != to for all entries)
8. Write manifest JSON in `migrate_initiative.py`-compatible format

**Success Criteria**:
- [ ] Manifest operations are complete relative to the inventoried filesystem (coverage proven via inventory + mapping evidence)
- [ ] 0 no-op moves
- [ ] All P-ISS-001 through P-ISS-009 issues addressed (P-ISS-010 excluded per decision)
- [ ] P-ISS-011 (verification relocation) included
- [ ] Manifest validates against `migrate_initiative.py` schema

### Task TK004.1: Remediation — Convention Compliance + `--report-path` Hardening

**Task ID**: `T104-PH001-ST007-AC004-TK004.1`

**Purpose**: Address all convention violations identified in the GATE-001 supplementary verification (`verification_T104-PH001-ST007-AC004-GATE-001_convention-compliance.md`). Consolidate developer-authored evidence into a single `dev-report_` artifact, correct file placements per Convention 4, and harden all `--report-path` scripts with smart defaults and workspace guards to prevent future misplacement.

**Deliverables**:
- `dev-report_T104-PH001-ST007-AC004_2026-02-23.md` (consolidated developer report in `AC004/dev-report/`)
- `prompt/scripts/report_output.py` (shared `--report-path` utility)
- Retrofitted `--report-path` handling in 5 scripts
- Removal of dead `path_utilities.py`

**Inputs Required**:
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/verification/verification_T104-PH001-ST007-AC004-GATE-001_convention-compliance.md` — finding register
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/raw/raw_T104-PH001-ST007-AC004-SES002.txt` — consultation session transcript
- `.claude/plans/plan_T104-PH001-ST007-AC004-SES002_tk004.1-remediation-and-hardening.md` — execution specification

**Steps**:

**WP-A: File Organization Remediation**
1. Create `AC004/dev-report/dev-report_T104-PH001-ST007-AC004_2026-02-23.md` — consolidate content from 7 source files
2. Move `analysis_T104-PH001-ST007-AC004_p-directory-readiness.md` from `AC004/analysis/` to `ST007/analysis/` (create directory)
3. Move `manifest_T104-PH001-ST007-AC004_p-migration.json` from AC004 root to `scripts/output/T104-PH001-ST007-AC004/ac004.1/`
4. Delete absorbed source files (3 from `AC004/verification/`, 4 from `scripts/output/.../ac004.1/`)
5. Delete empty `AC004/analysis/` directory

**WP-B: Script Output Renaming**
6. Rename 5 raw script outputs in `scripts/output/.../ac004.1/` from `verification_*` to `validation_*`

**WP-C: `--report-path` Hardening**
7. Create `prompt/scripts/report_output.py` — shared utility with `resolve_report_path()` (smart default + workspace guard)
8. Retrofit `validate_initiative_structure.py` — smart default to `scripts/output/validation/<id>/`
9. Retrofit `scaffold_initiative.py` — smart default to `scripts/output/scaffold/<id>/`
10. Retrofit `migrate_initiative.py` — smart default to `scripts/output/migration/<stem>/`
11. Retrofit `archive_manager.py` — smart default to `scripts/output/archive/`
12. Retrofit `migrations/migrate_adr_to_std.py` — add workspace guard (already has smart default)
13. Remove `prompt/scripts/path_utilities.py`

**WP-D: Cross-Reference Updates**
14. Update activity plan links register + context references (analysis file moved to ST007)
15. Update main verification file links register + frontmatter targets
16. Verify all internal links in dev-report point to correct current locations

**Success Criteria**:
- [ ] AC004 directory contains only: `plan_*`, `snotes/`, `verification/`, `dev-report/`, `raw/` (Convention 4 compliant)
- [ ] Zero files with unregistered prefixes in the workspace
- [ ] All 5 scripts produce reports to `scripts/output/` by default (no `--report-path` required)
- [ ] All 5 scripts reject `--report-path` values inside `artifacts/tasks/` with non-zero exit
- [ ] `scripts/output/.../ac004.1/` contains only: `manifest_*.json`, `report_*_dry-run.md`, 5x `validation_*` outputs
- [ ] `path_utilities.py` removed
- [ ] All cross-references valid (no broken links)
- [ ] `python3 -m py_compile` passes for all modified scripts

### GATE-001: Dry-Run + Client Approval

**Gate ID**: `T104-PH001-ST007-AC004-GATE-001`

**Entry Criteria**:
- TK002 (validator enhancement) completed
- TK003 (scaffold enhancement) completed
- TK004 (manifest) completed
- TK004.1 convention remediation completed and `--report-path` hardening verified
- Dry-run report produced by `migrate_initiative.py --dry-run`
- Inventory evidence produced and referenced by TK004
- Known-good rollback checkpoint captured and recorded (git commit hash or out-of-tree snapshot path)

**Reviewer**: Client

**Exit Criteria**:
- Client approves dry-run report
- No manifest errors flagged
- Dry-run directory tree matches expected target structure
- Rollback checkpoint evidence accepted as sufficient guarantee (DEC-AC004-005)

### Task TK005: Live Apply P Migration

**Task ID**: `T104-PH001-ST007-AC004-TK005`

**Purpose**: Execute the approved manifest against the live P directory.

**Deliverable**:
- Restructured `prompt/artifacts/tasks/P/**`
- Migration execution report
- Rollback manifest (generated by `migrate_initiative.py`)

**Steps**:
1. Confirm known-good rollback checkpoint is present and recorded (precondition)
2. Run `migrate_initiative.py --apply` with approved manifest
3. Verify execution report: all operations successful
4. Retain rollback manifest for recovery if needed
5. Capture/confirm pre-apply rollback snapshot exists (out-of-tree tarball or equivalent) per DEC-AC004-005
6. Manually create SSOT concept stub: `prompt/artifacts/tasks/P/ssot/concept_P-PROGRAM.md` (DEC-AC004-004)
7. Ensure required root `research/` exists (via manifest mkdir or scaffold) before strict validation

**Post-Apply Manual Steps (Required before TK006)**:
- [ ] Create `prompt/artifacts/tasks/P/ssot/concept_P-PROGRAM.md` (DEC-AC004-004)
- [ ] Confirm `prompt/artifacts/tasks/P/ssot/` contains both `sps_*.md` and `concept_*.md`

**Success Criteria**:
- [ ] All move/rename operations executed successfully
- [ ] No file loss (rollback manifest available)
- [ ] Directory structure matches target layout
- [ ] SSOT contains both `sps_*.md` and `concept_*.md`
- [ ] Required root directories exist, including `research/`

### Task TK006: Post-Apply Strict Validation

**Task ID**: `T104-PH001-ST007-AC004-TK006`

**Purpose**: Run the enhanced validator in strict mode against the restructured P directory to confirm 0 errors.

**Deliverable**:
- Validation report

**Steps**:
1. Run `validate_initiative_structure.py --strict` against `prompt/artifacts/tasks/P/`
2. Review report: 0 errors expected
3. Document any exceptions in allowlist if justified

**Success Criteria**:
- [ ] 0 errors in strict validation
- [ ] Any warnings documented and justified
- [ ] Validator confirms SSOT includes required `concept_*.md`

### Task TK007: Reference Update Pass

**Task ID**: `T104-PH001-ST007-AC004-TK007`

**Purpose**: Scan all files under `prompt/` for old P paths and rewrite to new canonical paths. Produce evidence that 0 old-path references remain.

**Deliverable**:
- Reference rewrite execution report
- Residual path scan (target: 0)

**Steps**:
1. Run `migrate_initiative.py` reference rewrite pass (integrated in manifest execution, or standalone pass)
2. Record rewrite safety cap used (`--max-files-changed`) and scope root used (`--scope-root`, default `prompt`)
3. Run residual scan: grep for old P paths across `prompt/`
4. Fix any residuals manually if needed
5. Produce evidence: `residual_paths=0`

**Success Criteria**:
- [ ] Reference rewrite pass executed
- [ ] Residual scan: 0 old P paths remain
- [ ] Evidence produced for GATE review

### GATE-002: Post-Migration Final Quality Gate

**Gate ID**: `T104-PH001-ST007-AC004-GATE-002`

**Entry Criteria**:
- TK005 (live apply) completed — P directory restructured
- TK006 (strict validation) completed — 0 errors in strict mode
- TK007 (reference update) completed — 0 stale P paths

**Reviewer**: Client

**Exit Criteria**:
- Client approves strict validation report (0 errors)
- Residual path scan: 0 stale P references across `prompt/`
- P SSOT contains both `sps_*.md` and `concept_*.md`
- Migration execution report + rollback manifest retained
- Activity plan task statuses all updated to `completed`

---

### Sub-Activity AC004.1: Delta Remediation + P Re-run (Revision 2)

**Sub-Activity ID**: `T104-PH001-ST007-AC004.1`

**Purpose**: Execute a bounded remediation + delta rerun of the P migration to align with program-level `P-STD-004` authority and the program greenlight disposition package (`P-PH000-ST001-AC004-GATE-003`, client label “GATE-002.1”).

**Sub-Activity Plan**:
- `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/plan_T104-PH001-ST007-AC004.1.md`

## VI. Links Register

| Link Type | Target | Path |
|:--|:--|:--|
| Plan (this file) | AC004 Activity Plan | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/plan_T104-PH001-ST007-AC004.md` |
| Plan | Stream Plan | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/plan_T104-PH001-ST007.md` |
| Sub-Activity Plan | AC004.1 Delta Remediation + P Re-run | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/plan_T104-PH001-ST007-AC004.1.md` |
| Analysis (input) | P Directory Readiness | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/analysis/analysis_T104-PH001-ST007-AC004_p-directory-readiness.md` |
| Proposal (authority) | Directory/Naming Proposal v3.4.0 | `prompt/artifacts/tasks/T104/workspace/PH001/ST002/AC000/proposal/proposal_T104-PH001-ST002-AC000_directory-naming-convention.md` |
| Verification (primary) | GATE-001 P Migration Readiness | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/verification/verification_T104-PH001-ST007-AC004-GATE-001_p-migration-readiness.md` |
| Verification (supplementary) | GATE-001 Convention Compliance | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/verification/verification_T104-PH001-ST007-AC004-GATE-001_convention-compliance.md` |
| Verification (supplementary) | GATE-001 Commissioning Readiness | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/verification/verification_T104-PH001-ST007-AC004-GATE-001_commissioning-readiness.md` |
| Evidence (consolidated) | Dev-Report | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/dev-report/dev-report_T104-PH001-ST007-AC004_2026-02-23.md` |
| Evidence (Gate-002 execution) | Dev-Report | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/dev-report/dev-report_T104-PH001-ST007-AC004_2026-02-24.md` |
| Evidence (manifest) | Migration Manifest | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/manifest_T104-PH001-ST007-AC004_p-migration.json` |
| Evidence (dry-run) | Dry-Run Report | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.1/report_T104-PH001-ST007-AC004_gate-001_dry-run.md` |
| Evidence (apply) | GATE-002 Live Apply Report | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.2/report_T104-PH001-ST007-AC004_gate-002_apply.md` |
| Evidence (rollback) | GATE-002 Rollback Manifest | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.2/rollback_manifest.json` |
| Evidence (validation) | TK006 Strict Post-Migration Validation | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.2/validation_T104-PH001-ST007-AC004_tk006_p-strict-post-migration-final.md` |
| Evidence (residual scan) | TK007 Residual Path Scan | `prompt/scripts/output/T104-PH001-ST007-AC004/ac004.2/validation_T104-PH001-ST007-AC004_tk007_residual-path-scan.md` |
| Raw (session transcript) | SES002 Consultation | `prompt/artifacts/tasks/T104/workspace/PH001/ST007/AC004/raw/raw_T104-PH001-ST007-AC004-SES002.txt` |
| Plan (execution spec) | TK004.1 Remediation + Hardening Plan | `.claude/plans/plan_T104-PH001-ST007-AC004-SES002_tk004.1-remediation-and-hardening.md` |

## VII. Changelog

| Version | Date | Type | Summary |
|:--|:--|:--|:--|
| v1.0.0 | 2026-02-22 | Initial | Activity plan created. 7 tasks + 1 gate. Source: ST007-SES002 consultation. |
| v1.1.0 | 2026-02-22 | Amendment | Commissioning update: locked roadmap/proposal/rollback decisions; corrected validator pre-migration semantics; removed unsupported manifest `create` operations; made P inventory authoritative; added manual concept stub step; strengthened GATE-001 entry criteria. |
| v2.0.0 | 2026-02-24 | Major | Plan amendment: (1) Added TK004.1 (convention compliance remediation + --report-path hardening) between TK004 and GATE-001; (2) Updated GATE-001 depends-on to include TK004.1 and added TK004.1 entry criterion; (3) Added GATE-002 after TK007 as post-migration final quality gate; (4) Updated context reference for analysis file moved from AC004/analysis/ to ST007/analysis/; (5) Added supplementary verification and session transcript links. Source: SES002 consultation (raw_T104-PH001-ST007-AC004-SES002.txt). |
| v2.1.0 | 2026-02-24 | Major | Executed TK005-TK007: live migration apply completed, manual `concept_P-PROGRAM.md` created, strict validation reached 0 errors after targeted research-report relocation remediation, residual stale-path scan reached 0; task register statuses updated and Gate-002 evidence package linked for `LLM_Reviewer` verification handoff. |
| v2.2.0 | 2026-03-02 | Amendment | Registered follow-on Sub-Activity `AC004.1` for delta remediation + P rerun revision 2 driven by program-level `P-STD-004` authority and GATE-003 (“GATE-002.1”) dispositions. |
